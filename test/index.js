const chai = require('chai');
const assert = chai.assert;
const expect = chai.expect;

describe('Tests', function () {
    it('Tests run', function () {
        assert.isTrue(true, 'Tests run');
    })
})
